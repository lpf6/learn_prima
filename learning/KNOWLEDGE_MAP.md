# 知识脉络图

本文档展示 Prima.cpp 学习过程中各知识点的关联关系，帮助你建立完整的知识体系。

## 核心知识架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           最终目标                                           │
│         维护 Prima.cpp + 添加新模型 + 处理多平台兼容性                         │
└─────────────────────────────────────────────────────────────────────────────┘
                                    ▲
                                    │
        ┌───────────────────────────┼───────────────────────────┐
        │                           │                           │
        ▼                           ▼                           ▼
┌───────────────┐          ┌───────────────┐          ┌───────────────┐
│   GPU 优化    │          │   CPU 优化    │          │   模型架构    │
│               │          │               │          │               │
│ • CUDA 编程   │          │ • SIMD 指令   │          │ • Transformer │
│ • 架构适配    │          │ • 多平台兼容  │          │ • GGML 计算图 │
│ • 内核优化    │          │ • 量化实现    │          │ • 模型添加    │
└───────────────┘          └───────────────┘          └───────────────┘
        │                           │                           │
        └───────────────────────────┼───────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           基础知识层                                         │
│                                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │
│  │ C++ 编程    │  │ 并行计算    │  │ 计算机体系  │  │ 深度学习    │       │
│  │             │  │             │  │ 结构       │  │ 基础        │       │
│  │ • 模板      │  │ • 线程模型  │  │ • 内存层次  │  │ • 神经网络  │       │
│  │ • 内存管理  │  │ • 同步机制  │  │ • 缓存原理  │  │ • 注意力    │       │
│  │ • RAII     │  │ • 数据竞争  │  │ • SIMD     │  │ • 词嵌入    │       │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘       │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 知识点依赖关系

### 第一层：基础概念

```
并行计算基础
    │
    ├──► CUDA 线程模型 ──► Warp 概念 ──► Warp 归约
    │         │
    │         └──► 内存层次 ──► 共享内存 ──► 分块算法
    │                              │
    │                              └──► 内存合并 ──► 性能优化
    │
    └──► 同步机制 ──► Barrier ──► 原子操作
                              │
                              └──► Warp 同步 ──► Shuffle 指令
```

### 第二层：GPU 架构

```
GPU 硬件架构
    │
    ├──► 计算能力 (SM 75/80/86/89/90)
    │         │
    │         ├──► Turing (75) ──► INT8 Tensor Core
    │         │
    │         ├──► Ampere (80) ──► TF32, BF16, 硬件归约
    │         │
    │         ├──► Ada (89) ──► FP8
    │         │
    │         └──► Hopper (90) ──► 线程块集群
    │
    └──► 条件编译 ──► 架构适配 ──► 多架构支持
```

### 第三层：量化技术

```
量化理论
    │
    ├──► 线性量化 ──► 对称/非对称
    │         │
    │         └──► 块量化 ──► Q4_0, Q4_1, Q8_0
    │
    ├──► K-Quant ──► 分层缩放 ──► Q4_K, Q6_K
    │
    └──► IQ 系列 ──► 重要性加权 ──► IQ4_NL
              │
              └──► 实现
                    │
                    ├──► CPU 量化 (ggml-quants.c)
                    │
                    └──► CUDA 量化 (quantize.cu)
                              │
                              └──► dp4a 指令 ──► 向量点积
```

### 第四层：核心内核

```
LLM 推理核心算子
    │
    ├──► 矩阵乘法 (MMQ)
    │         │
    │         ├──► 分块策略 ──► 共享内存复用
    │         │
    │         ├──► 量化矩阵乘 ──► 反量化 + GEMM
    │         │
    │         └──► Tensor Core ──► WMMA 接口
    │
    ├──► Flash Attention
    │         │
    │         ├──► 在线 Softmax ──► 数值稳定性
    │         │
    │         ├──► 分块计算 ──► 内存优化
    │         │
    │         └──► WMMA 实现 ──► Tensor Core 加速
    │
    └──► 其他算子
              │
              ├──► RMSNorm ──► Warp 归约
              │
              ├──► RoPE ──► 三角函数计算
              │
              └──► Softmax ──► 数值稳定性
```

### 第五层：CPU 优化

```
CPU SIMD
    │
    ├──► x86 架构
    │         │
    │         ├──► SSE (128-bit) ──► 基础 SIMD
    │         │
    │         ├──► AVX (256-bit) ──► FP16 转换
    │         │
    │         ├──► AVX2 ──► 整数 SIMD + FMA
    │         │
    │         └──► AVX-512 (512-bit) ──► 掩码操作
    │
    └──► ARM 架构
              │
              ├──► NEON (128-bit) ──► 基础 SIMD
              │
              └──► SVE ──► 可变长度向量
```

### 第六层：模型架构

```
Transformer 架构
    │
    ├──► 模型结构
    │         │
    │         ├──► Embedding ──► 词嵌入查找
    │         │
    │         ├──► Attention ──► QKV 投影 + Attention 计算
    │         │
    │         └──► FFN ──► SwiGLU 激活
    │
    ├──► GGML 计算图
    │         │
    │         ├──► 张量定义 ──► ggml_tensor
    │         │
    │         ├──► 操作类型 ──► ggml_op
    │         │
    │         └──► 图构建 ──► ggml_cgraph
    │
    └──► GGUF 格式
              │
              ├──► 元数据 ──► 模型参数
              │
              └──► 张量数据 ──► 权重存储
```

### 第七层：后端架构

```
GGML 后端抽象
    │
    ├──► 后端接口
    │         │
    │         ├──► ggml_backend ──► 后端抽象
    │         │
    │         └──► ggml_backend_buffer ──► 内存管理
    │
    └──► 具体后端
              │
              ├──► CPU ──► SIMD 优化
              │
              ├──► CUDA ──► NVIDIA GPU
              │
              ├──► Metal ──► Apple Silicon
              │
              ├──► Vulkan ──► 跨平台 GPU
              │
              └──► ROCm/HIP ──► AMD GPU
```

### 第八层：后端详解

```
多后端对比
    │
    ├──► CUDA
    │         │
    │         ├──► 最佳性能
    │         │
    │         ├──► Tensor Core
    │         │
    │         └──► 成熟生态
    │
    ├──► Metal
    │         │
    │         ├──► Apple Silicon
    │         │
    │         ├──► 统一内存
    │         │
    │         └──► 能效比高
    │
    ├──► Vulkan
    │         │
    │         ├──► 跨平台
    │         │
    │         ├──► 底层访问
    │         │
    │         └──► 移动支持
    │
    └──► ROCm
              │
              ├──► AMD GPU
              │
              ├──► HIP 移植
              │
              └──► Matrix Core
```

### 第九层：实践与工具

```
实践项目
    │
    ├──► 推理服务
    │         │
    │         ├──► API 设计
    │         │
    │         └──► 批处理调度
    │
    ├──► 自定义内核
    │         │
    │         ├──► 需求分析
    │         │
    │         └──► 性能优化
    │
    └──► 模型量化
              │
              ├──► 校准流程
              │
              └──► 精度评估

工具生态
    │
    ├──► 构建工具
    │         │
    │         ├──► CMake
    │         │
    │         └──► 编译器
    │
    ├──► 调试工具
    │         │
    │         ├──► GDB/CUDA-GDB
    │         │
    │         └──► 内存检测
    │
    └──► 性能分析
              │
              ├──► Nsight
              │
              └──► rocprof

故障排查
    │
    ├──► 常见错误
    │
    ├──► 调试策略
    │
    └──► 性能调试
```

## 知识点交叉引用

### CUDA 与 CPU SIMD 的对应关系

| CUDA 概念 | CPU SIMD 对应 | 说明 |
|-----------|--------------|------|
| Warp (32线程) | SIMD 向量 | 并行处理单元 |
| Shared Memory | L1 Cache | 快速暂存区 |
| Warp 归约 | 水平操作 | 向量内归约 |
| Shuffle | 重排指令 | 数据交换 |
| __syncthreads() | 内存屏障 | 同步点 |
| CUDA 流 | 多线程 | 并发执行 |
| CUDA 事件 | 同步原语 | 依赖管理 |

### 量化与内核的关系

```
量化类型选择
    │
    ├──► Q4_0/Q4_1 ──► mmq.cu (通用矩阵乘)
    │
    ├──► Q4_K ──► mmq.cu (优化矩阵乘)
    │
    └──► 反量化 ──► dequantize.cuh
              │
              └──► vecdotq.cuh (向量点积)
                        │
                        └──► dp4a 指令 (INT8 加速)
```

### 架构适配与性能优化

```
架构特性
    │
    ├──► SM 75 (Turing)
    │         │
    │         └──► INT8 MMA ──► 量化矩阵乘加速
    │
    ├──► SM 80 (Ampere)
    │         │
    │         ├──► TF32 ──► FP32 矩阵乘加速
    │         │
    │         └──► 硬件归约 ──► Warp 归约优化
    │
    ├──► SM 89 (Ada Lovelace)
    │         │
    │         └──► FP8 ──► 更高吞吐量
    │
    └──► SM 90 (Hopper)
              │
              └──► 线程块集群 ──► 分布式推理
```

### 后端选择与硬件匹配

```
硬件平台
    │
    ├──► NVIDIA GPU
    │         │
    │         ├──► CUDA (首选)
    │         │
    │         └──► Vulkan (备选)
    │
    ├──► AMD GPU
    │         │
    │         ├──► ROCm (首选, Linux)
    │         │
    │         └──► Vulkan (备选, Windows)
    │
    ├──► Apple Silicon
    │         │
    │         └──► Metal (唯一选择)
    │
    ├──► Intel GPU
    │         │
    │         └──► Vulkan
    │
    └──► 移动 GPU
              │
              └──► Vulkan (Android) / Metal (iOS)
```

## 学习路径建议

### 路径 A：GPU 优化方向

```
CUDA 基础 ──► GPU 架构 ──► 量化技术 ──► CUDA 内核 ──► 后端兼容性
     │              │            │            │
     └──────────────┴────────────┴────────────┘
                          │
                          ▼
              能够优化和开发 CUDA 内核
```

### 路径 B：模型支持方向

```
量化技术 ──► LLM 架构 ──► 模型添加
     │            │
     └────────────┘
          │
          ▼
    能够添加新模型支持
```

### 路径 C：跨平台方向

```
CUDA 基础 ──► CPU SIMD ──► 后端兼容性
     │            │            │
     └────────────┴────────────┘
               │
               ▼
     能够处理多平台兼容性
```

### 路径 D：后端开发方向

```
GPU 架构 ──► 后端详解 ──► 后端选择
     │            │
     └────────────┘
          │
          ▼
    能够开发和优化后端
```

### 路径 E：实践导向

```
基础知识 ──► 工具生态 ──► 实践项目 ──► 故障排查
     │            │            │            │
     └────────────┴────────────┴────────────┘
                          │
                          ▼
                  能够独立完成项目
```

## 常见问题与知识点关联

### Q1: 为什么量化后推理更快？

```
量化技术 ──► 内存带宽减少 ──► 数据传输加速
     │
     └──► INT8 运算 ──► dp4a 指令 ──► 计算加速
              │
              └──► Tensor Core ──► 矩阵乘加速
```

### Q2: Flash Attention 为什么省内存？

```
Flash Attention
     │
     ├──► 分块计算 ──► 不存储完整注意力矩阵
     │
     └──► 在线 Softmax ──► 增量更新结果
              │
              └──► 内存复杂度: O(N) vs O(N²)
```

### Q3: 如何支持新 GPU 架构？

```
新架构支持
     │
     ├──► 计算能力检测 ──► ggml_cuda_info()
     │
     ├──► 条件编译 ──► #if __CUDA_ARCH__ >= XXX
     │
     └──► 内核优化 ──► 使用新特性指令
```

## 实践项目与知识点对应

| 实践项目 | 所需知识点 | 难度 |
|---------|-----------|------|
| 编写向量加法内核 | CUDA 基础 | ★ |
| 优化矩阵转置 | 共享内存、内存合并 | ★★ |
| 实现并行归约 | Warp 归约、同步 | ★★ |
| CUDA 流和异步操作 | CUDA 流、事件 | ★★ |
| 性能分析与优化 | Nsight、性能工具 | ★★ |
| 调试 CUDA 程序 | cuda-memcheck、GDB | ★★ |
| 添加量化类型 | 量化原理、CPU/CUDA 实现 | ★★★ |
| 优化矩阵乘内核 | 分块策略、Tensor Core | ★★★ |
| 实现 Flash Attention | 在线 Softmax、分块计算 | ★★★★ |
| 构建推理服务 | API 设计、批处理 | ★★★★ |
| 自定义内核开发 | 需求分析、性能优化 | ★★★★ |
| 模型量化实践 | 校准、精度评估 | ★★★★ |
| 添加新模型 | LLM 架构、计算图构建 | ★★★★ |
| 添加新后端 | 后端接口、硬件特性 | ★★★★★ |
| 性能基准测试 | 测试方法、结果分析 | ★★★ |

## 总结

理解知识点之间的关联可以帮助你：

1. **建立知识体系**：不是孤立地学习每个概念
2. **理解设计决策**：为什么代码这样写
3. **快速定位问题**：遇到问题时知道涉及哪些知识点
4. **举一反三**：将学到的知识应用到新场景
5. **选择合适工具**：根据硬件选择最佳后端
6. **独立解决问题**：运用调试和排查技巧

## 新增内容总结

本次完善新增以下内容：

### 新增模块
- **00-prerequisites**: 前置知识（C++、并行计算、深度学习）
- **11-projects**: 实践项目（推理服务、自定义内核、量化实践、性能测试）
- **12-tools-ecosystem**: 工具与生态（构建工具、调试工具、性能分析）
- **13-troubleshooting**: 故障排查（常见错误、调试策略、FAQ）

### 扩展内容
- **01-cuda-basics**: CUDA 流、性能工具、调试技术
- **07-backend-compatibility**: Metal、Vulkan、ROCm 后端详解、后端选择指南

### 更新文档
- **README.md**: 更新教程结构和学习路线
- **KNOWLEDGE_MAP.md**: 添加后端详解、实践项目、工具生态知识点
